-- +goose Up
-- Initial migration placeholder. Real schema arrives in subsequent
-- migrations as each feature lands. Keeping a non-empty Up here makes
-- the goose machinery exercise a real statement on a fresh database.
SELECT 1;

-- +goose Down
SELECT 1;
