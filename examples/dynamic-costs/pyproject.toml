[project]
name = "orla-example-dynamic-costs"
version = "0.1.0"
description = "A toy cost service Orla polls for time-varying backend prices."
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.122.1",
    "uvicorn==0.42.0",
    "pydantic==2.13.4",
]

[dependency-groups]
dev = [
    "ruff==0.15.20",
    "ty==0.0.55",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
