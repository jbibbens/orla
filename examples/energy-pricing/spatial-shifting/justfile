# Tasks for the spatial shifting experiment. Requires uv.

# List recipes.
default:
    @just --list

# Serve regional electricity prices as Orla cost sources.
prices:
    uv run uvicorn price_service:app --host 127.0.0.1 --port 9100

# Serve the stand-in inference backend.
backend:
    uv run uvicorn sim_backend:app --host 127.0.0.1 --port 9200

# Serve the stage mapper that routes the dynamic arm (port 8092).
mapper:
    uv run uvicorn cheapest_mapper:app --host 127.0.0.1 --port 8092

# Run the experiment (needs both services and a running Orla daemon).
run:
    uv run experiment.py

# Format the code with ruff.
fmt:
    uv run ruff format .

# Lint with ruff (use `just lint --fix` to apply fixes).
lint *args:
    uv run ruff check {{ args }} .

# Type-check with ty.
typecheck:
    uv run ty check .

# Read-only checks, the way CI would run them.
check:
    uv run ruff format --check .
    uv run ruff check .
    uv run ty check .
